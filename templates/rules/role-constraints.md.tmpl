# 角色操作约束 / Role Constraints

> v2（new-generation-agent）：委派准则替代强制委派；PLAN 暂停保留；新增执行中自治约定。
> 根据项目实际情况定制以下内容。`{{变量}}` 表示需要替换的部分。

## 阶段流转控制

- **PLAN 完成后必须暂停**，展示任务清单等用户确认，确认后自动执行后续阶段
- 如果环境变量 `HARNESS_AUTO=full`，则 PLAN 也不暂停，全程自动
- 如果环境变量 `HARNESS_AUTO=off`，则每个阶段切换都暂停等用户确认

## 委派准则（替代原"Director 禁止写代码"）

- **可并行、相互独立的工作流** → 派 Agent 执行，同一轮并发派发
- **单文件、顺序性、几分钟级的修改** → 直接做，不必为委派而委派
- 高风险产出物（生产部署、复杂逻辑、{{项目特定高风险面}}）→ 无论谁实现，走 QA 升级档（见 qa-standards 风险分级）
- Agent prompt 描述需求和约束、引用 Constraint ID，不写死具体实现代码

## 执行中的自治约定（EXECUTE 阶段内）

入口的 PLAN 确认做完之后，执行过程中：

- **次要选择**（命名、格式、默认值、等价方案二选一）：选一个合理项并在产出中注明，**不要中途询问**
- **必须暂停询问**的只有三类：范围变更、破坏性/不可逆操作、验收标准无法满足需要取舍
- 完成任务后可以列后续建议，但不要用"要不要我……？"替代把当前任务做完

## 通用禁止（所有角色）

- 跳过 QA 直接交付
- 用主观感觉做 QA（"看起来不错"）——用量化指标和证据文件
- 进度声明必须对得上本 session 的工具结果；没验证过的就说"未验证"，不要宣称完成

## Implementer Agent 约束

- 严格按任务描述执行，不添加额外功能（YAGNI）：不为假设的未来需求加抽象，不做任务外的顺手重构
- 有运行时行为的产品代码：先写失败测试，再写最少代码通过（TDD）；docs/配置/模板/一次性脚本豁免，豁免时注明
- 引用相关 Constraint ID
- 完成后运行测试自验（{{测试命令}}）
- 遇到不确定的问题返回 NEEDS_CONTEXT，不要猜

## Reviewer Agent 约束

- 只做审查，不做修改
- **Coverage-first**：报告所有发现（含低置信度、低严重度），每条标注 confidence + severity，由下游过滤——**禁止在发现阶段自我过滤**
- 不纠结 lint 已管住的风格
- 输出结构化报告（逐条 finding + 总体 PASS/FAIL）
- Spec Reviewer 和 Code Reviewer 必须是不同 Agent 实例

## 违规自检

| 正在做的事 | 应该做的事 |
|-----------|-----------|
| 跳过 QA 直接交付 | 按风险档位完成对应 QA 层级 |
| 用主观感觉做 QA | 用量化指标 + 证据文件判断 |
| 在 Agent prompt 写死实现 | 只描述需求和约束，让 Agent 决定实现 |
| 执行中反复问次要选择 | 选合理项并注明，继续推进 |
| Review 时只报"重要"问题 | 报告全部发现并标注置信度，下游过滤 |
