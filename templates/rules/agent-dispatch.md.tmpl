# Agent 派发规范 / Agent Dispatch Rules

## 修复类 Agent

Prompt 必须包含：
1. 引用 Constraint ID（如 `C-UI-03`）
2. 指向 docs/constraints.md
3. 不包含具体实现代码（让 Agent 自己决定）
4. 要求自验（运行测试确认通过）

```markdown
# 示例
修复 C-{{area}}-{{number}} 违规。
读取 docs/constraints.md 中该约束的完整描述。
扫描 {{范围}} 中所有不符合该约束的地方，全部修复。
修复后运行 {{测试命令}} 确认通过。
```

## 派 Reviewer 前：先冻结被审对象

**派出去之前必须先把被审的东西定住**，并把版本标识一并交给 reviewer：commit sha、
stash 引用、或一份只读快照 + 内容 sha256。**review 未回之前，主循环只做只读操作。**

不这么做的真实后果：一位 reviewer 读到的文件在它复审期间被并发改了 4 次，其中一次是
半写入的不可编译中间态；另一位基于早于门禁的旧快照，把"候选不含本轮改动"报成阻断项。
两轮 review 白烧，而报告本身看不出问题——它们如实描述了各自看到的那个瞬间。

Santa 双独立审查定义了"两个 reviewer 上下文隔离、相同 rubric"，但如果没定义
**审的是哪个版本**，隔离就只是形式：两个 reviewer 可能在审两份不同的代码。

## Reviewer 的边界是纪律，不是沙箱

subagent 的工具集通常是完整的 —— reviewer 手里往往真的握着可写工具。
"只读""只审"必须在 prompt 里**显式声明**，并列出禁止的动作类别（写文件、提交、
部署、改配置、跑破坏性命令）。不要假设工具层会替你拦住。

## Spec Compliance Reviewer Agent

独立 session，接收：
- 需求规格文档
- Agent 的代码变更（diff 或文件内容）**及其版本标识**
- 明确的只读边界声明

输出：结构化 PASS/FAIL 清单，逐项对照 spec。

## Code Quality Reviewer Agent

独立 session，在 Spec Review 通过后执行。接收：
- 代码变更及其版本标识
- 项目编码规范
- 明确的只读边界声明

输出：APPROVE / REQUEST_CHANGES + 分级 issues。

### Review 的固定维度

每次 review 至少覆盖这几项，缺项要说明为什么不适用：

| 维度 | 判据 |
|---|---|
| 需求符合 | 逐条对照 spec，不靠印象 |
| 正确性 | 边界、错误路径、并发 |
| **测试有效性** | **没有变异验证的绿灯一律判 FAIL** —— 断言存在不等于断言有约束力 |
| **回滚可行** | 出事能不能退回去，退回的路径验证过没有 |
| 安全与泄漏面 | 凭据、越权、对外可见产物 |

### 判定纪律

**拿不准就写 FAIL，并说明需要什么证据才能翻案。** 不要为了让流程往前走而放行 ——
reviewer 放行的成本由下游承担，而下游通常没有你此刻掌握的上下文。

## QA Agent

独立 session，执行 Verification Loop：
1. Build → Type → Lint → Test → Security → Diff
2. 输出量化报告
3. 全部 PASS 才算通过

## 模型选择

| 任务类型 | 推荐模型 |
|---------|---------|
| 机械性实现（单文件、规格清晰） | Haiku / Sonnet |
| 集成性实现（多文件、需要判断） | Sonnet / Opus |
| Code Quality Review | Opus |
| Spec Compliance Review | Sonnet |
