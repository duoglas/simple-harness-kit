# Model Profile — 新一代模型行为指引（双轨）

> new-generation-agent 模板。厂商特定指引只写在这里，其他 rules 保持 model-agnostic。
> 按当前运行的模型读对应轨；guard 模式（strict/light）由 hook 自动检测（scripts/hooks/guard-mode.js），不需要 AI 处理。
> init 时按项目实际情况保留/裁剪；`{{变量}}` 表示需替换的部分。

## 共享层（两家通用）

- **任务描述结构**：目标 / 成功标准 / 停止条件 / 输出格式。写任务给 Agent 时用这个结构，不枚举执行步骤。
- **规则冲突零容忍**：发现两条规则冲突时，不要自行调和——按 rules 文件声明的优先级执行，并把冲突记入 session log（这是规则 bug，走 F1-F5）。
- **深度控制交给参数**：思考深度用模型参数/配置调，不在 prompt 里写"仔细思考""认真检查"类措辞。
- **进度可信**：进度声明必须对得上本 session 的工具结果；未验证的说"未验证"。

## Claude 轨（Claude Code：Opus 4.7+ / Sonnet 5+ / Fable）

- **effort**：常规任务 `high`；复杂 agentic/coding 任务 `xhigh`。
- **行为矫正**：
  - 有足够信息就行动——不要反复重推已确认的事实，不要罗列不打算执行的选项
  - 不做任务外的整理——修 bug 不顺手重构，不为假设需求加抽象
  - 用户在描述问题/提问时，交付物是你的评估；先报告，等要求再动手修
- **工具触发**：这代模型对子代理/记忆/自定义工具偏保守。需要它用某个能力时，把**触发条件**写进该能力的 description（"当……时调用"），比系统提示里的命令有效。
- **委派**：可并行的独立工作流大胆派子代理（同轮并发）；单文件顺序修改直接做。

## GPT 轨（Codex：GPT-5.6+ / o5+）

- **reasoning_effort**：走 Codex 配置 `model_reasoning_effort`；复杂任务调高、简单任务调低。
- **eagerness 控制**（与 Claude 相反方向——这代 GPT 默认过度探索）：
  - 先做最少必要的搜索/读取；前几个结果已收敛就停止
  - 只有验证失败或出现新未知时才继续查
  - 探索类操作给自己设预算上限；到限即停并汇报缺口，不编造
- **verbosity**：状态更新一两句话；代码和产物保持完整可读——压的是过程叙述，不是交付物。
- **冲突指令代价**：GPT 会花 reasoning token 调和冲突规则。发现冲突立即按优先级取一头，不要两头兼顾。

## 项目定制区

- {{项目特定的模型偏好，如固定 guard_mode、指定 effort 档位等；没有则删除本节}}

## 维护说明

- 新模型家族上线时：先更 `scripts/hooks/guard-mode.js` 的判定规则，再在本文件加轨或并轨。
