# v0.11.0 draft: Phase 2 Quality Engineering Gate

This is the draft release note for the Phase 2 PR. It should be used after the PR is merged and tagged; do not backfill these capabilities into v0.10.0.

## Short version

SHK now treats AI delivery as a quality engineering problem, not just a checklist problem.

The agent must be able to explain:

1. what the iteration is supposed to change;
2. how that change will be tested;
3. whether the tests actually cover the changed risk;
4. whether broken critical behavior would be caught;
5. whether the work can be handed off now.

If the answer is “tests passed, but they did not prove the change,” SHK reports `NOT_SUFFICIENT` and blocks delivery.

## What users will notice

When a user asks Claude Code or Codex to implement a feature, SHK now expects the agent to work like this:

```text
First I will write the iteration spec and identify the risky paths.
Then I will check the project’s existing tests.
If the target app has no E2E for this path, I will generate the smallest useful one.
After it passes, I will check whether it is sufficient: positive path, negative path, assertions, fresh evidence, and mutation/fault proof.
Only then will I say the work is ready.
```

The user should not need to remember `node scripts/shk.js ...` commands. Those commands are backend probes for the AI workflow.

## Major changes

### 1. Spec-driven iteration gate

Medium/high/release tasks depend on `.harness/iteration-spec.json`.

The spec needs to map:

- requirements;
- proposed approach;
- risks;
- traffic paths;
- test plan;
- acceptance evidence.

If the spec is missing, empty, or not connected to the task, the work is not ready to start or deliver.

### 2. Target-app E2E bootstrap

When SHK is installed into a new application project, the AI should inspect the project and decide what kind of E2E makes sense:

- Web or fullstack app: usually Playwright, unless Cypress already exists.
- API service: API E2E, not a fake browser test.
- Unknown project: ask one concrete startup question instead of guessing broadly.

The goal is not to generate lots of tests. The goal is to generate the smallest test that proves the risky path.

### 3. E2E sufficiency

E2E PASS no longer means “safe to ship.”

SHK checks whether the E2E has proof strength:

- real assertions;
- positive path;
- negative or blocking path;
- structured evidence from the current run;
- mapping back to the iteration spec;
- not smoke-only;
- mutation/fault evidence that broken behavior is caught.

Fake E2E, `echo ok`, opening only the home page, or checking only `/health` cannot make medium/high/release work READY.

### 4. Test effectiveness gate

`shk test effectiveness` looks at the relationship between the spec and the tests.

It asks the engineering question users actually care about:

```text
Do these tests prove this change, or did they merely run?
```

### 5. Bounded repair loop

When tests fail or evidence is insufficient, the AI enters a controlled repair loop:

- at most 3 rounds;
- one failure point per round;
- rerun the smallest relevant test;
- stop and explain if no progress is made;
- no auto-push, auto-tag, release, unsafe reset, or bypass.

### 6. Hook gates block fake readiness

Delivery and verification gates now treat insufficient proof as blocking. They also keep degraded runtime status visible instead of allowing the report to call it PASS.

### 7. Release-ready evidence is enforced by the release gate

`tests/pre-release-check.sh` and `shk verify --risk release` now use the same required evidence vocabulary:

- `tests/run.js`;
- 17/18/19 dogfood evidence;
- Codex runtime smoke and selftest;
- `shk doctor`;
- clean working tree;
- local `master/main` synced with upstream.

For release readiness, only `PASS` is acceptable. `SKIP`, `DEGRADED`, `WARN`, and `FAIL` all block the release. `SKIP_SYNC_CHECK=1` is a local diagnostic switch only; it cannot produce release-ready `PASS`.

## Dogfood evidence

The Phase 2 test matrix covers:

- fake E2E is blocked;
- smoke-only E2E is not sufficient;
- missing spec blocks medium delivery;
- generated target-app tests catch mutation;
- AI Harness workflow can generate target-app E2E;
- spec-driven acceptance catches broken behavior;
- real OSS dogfood on TodoMVC and an Express API fixture;
- upstream npm install / original CI dogfood;
- browser E2E with mutation proof where runtime is available.

Latest PR evidence from the 2026-06-08 release rerun:

```text
node tests/run.js: 218 passed, 0 failed
run-all matrix: 17 PASS / 0 SKIP / 0 FAIL in this sandbox
browser E2E: validated separately where browser runtime was available
security scan: PASS
Codex runtime smoke: DEGRADED / SKIP, not runtime PASS
release gate: NOT_READY until dogfood, Codex runtime, doctor, clean tree, and upstream sync all report PASS in the release environment
```

17/18/19 dogfood evidence:

| Script | Exit | Last line | JSON artifact status | Release meaning |
|---|---:|---|---|---|
| `tests/scripts/17-oss-dogfood-validation.sh` | 0 | `PASS: real OSS dogfood caught frontend and API mutations` | `PASS` at `/private/tmp/shk-oss-dogfood-artifacts/phase2-oss-dogfood-result.json` | SHK generated spec/E2E/effectiveness evidence on real TodoMVC and Express API copies, and killed real-source mutations. |
| `tests/scripts/18-upstream-ci-dogfood.sh` | 0 | `PASS: upstream npm install / original CI dogfood completed` | `PASS` at `/private/tmp/shk-upstream-ci-artifacts/phase2-upstream-ci-dogfood-result.json` | Both OSS projects completed npm install/ci. Original CI proof remains `NO_PROOF` for both projects, so this is dependency/install evidence, not effective upstream CI evidence. |
| `tests/scripts/19-browser-e2e-dogfood.sh` | 0 | `PASS: real browser E2E caught TodoMVC mutation` | `PASS` at `/private/tmp/shk-browser-e2e-artifacts/phase2-browser-e2e-dogfood-result.json` | Headless Playwright Chromium exercised the real TodoMVC page and failed after the same source mutation. |

`run-all.sh` dimension summary: all 17 scripted dimensions were true `PASS`; there were no `SKIP-计绿` dimensions and no `FAIL` dimensions. Optional `shellcheck` was not installed and was skipped as a non-matrix precheck only.

Codex smoke reproducibility:

```text
codex --version: codex-cli 0.137.0
bash tests/codex-smoke.sh: exit 0, DEGRADED because project .codex/hooks.json command was not verified
CODEX_REQUIRED=1 bash tests/codex-smoke-selftest.sh: exit 1, because the project sentinel hook did not execute
bash tests/scripts/11-codex-smoke-contract.sh: PASS, fake-Codex contract evidence only
```

The live Codex run and the fake-Codex contract test prove different things. Live Codex evidence proves only the degraded runtime boundary above. The fake-Codex contract test proves the smoke scripts classify required/unverified and simulated bad-hook cases correctly; it does not prove real Codex executed project hooks.

## What is still not claimed

This release does not claim formal test coverage proof.

It also does not claim Codex `exec` runtime hook execution is fully proven in every environment. If runtime is `DEGRADED`, SHK must report it as degraded and must not use it as release-ready runtime evidence.
